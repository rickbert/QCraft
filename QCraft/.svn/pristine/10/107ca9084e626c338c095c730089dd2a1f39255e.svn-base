package skills;

import industry.Archaeology;
import industry.Farming;
import industry.Fishing;
import industry.Mining;
import industry.Woodcutting;

import org.bukkit.entity.Player;

import combat.Archery;
import combat.Swords;
import combat.Toughness;

public enum SkillType {
	ARCHAEOLOGY, ARCHERY, FARMING, FISHING, MINING, SWORDS, TOUGHNESS, WOODCUTTING;
	
	public static Skill newSkill(SkillType skill, Player player, int level, int exp) {
		switch (skill) {
		case ARCHAEOLOGY:
			return new Archaeology(player, level, exp);
		case ARCHERY:
			return new Archery(player, level, exp);
		case FARMING:
			return new Farming(player, level, exp);
		case FISHING:
			return new Fishing(player, level, exp);
		case MINING:
			return new Mining(player, level, exp);
		case SWORDS:
			return new Swords(player, level, exp);
		case TOUGHNESS:
			return new Toughness(player, level, exp);
		case WOODCUTTING:
			return new Woodcutting(player, level, exp);
		default:
			return null;
		}
	}
	
	public static SkillType getSkillType(String skillType) {
		for (SkillType skill : SkillType.values()) {
			if (skill.name().toLowerCase().equals(skillType.toLowerCase())) {
				return skill;
			}
		}
		return null;
	}
}
